@page "{maNCC:int}"
@model KIMPACK.Pages.Suppliers.DeleteModel
@{
    ViewData["Title"] = "Xóa nhà cung cấp";
}

<div class="container mt-5">
    <div class="card border-danger">
        <div class="card-header bg-danger text-white text-center">
            <h3>Xóa nhà cung cấp</h3>
        </div>
        <div class="card-body">
            <p>Bạn có chắc chắn muốn xóa nhà cung cấp với mã <strong>@Model.MaNCC</strong> không?</p>
            <p>Nếu bạn xóa, mọi dữ liệu liên quan đến nhà cung cấp này sẽ không thể khôi phục.</p>
        </div>
        <div class="card-footer text-center">
            <form method="post">
                <button type="submit" class="btn btn-danger">
                    <i class="fa fa-trash"></i> Xóa
                </button>
                <a href="/Suppliers/Index" class="btn btn-secondary">
                    <i class="fa fa-arrow-left"></i> Quay lại
                </a>
            </form>
        </div>
    </div>
</div>